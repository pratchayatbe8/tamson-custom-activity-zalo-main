# Custom Node.js Activity

This project is a custom Node.js activity designed to process and send Zalo for Tamson Marketing Cloud Journey Builder

## Prerequisites

* **Node.js**: Ensure Node is installed (`node -v`).
* **npm**: Ensure npm is installed (`npm -v`).

## Installation

npm install

## Health

/health should return "OK ZALO"